Run the shell scripts in the respective folders for automated training with checkpoint save and resume. Result aggregator scripts generate the aggregate mean and standard deviation for the runs based on which datasets have been completed so far. 
